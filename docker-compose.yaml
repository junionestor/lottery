services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - .:/var/www/html
    environment:
      - APP_ENV=development
    command:  ["php", "-S", "0.0.0.0:8000", "-t", "/var/www/html/routes/"]
